1.What is the difference between var,let and const?
ans=> 
var=not block scoped ,reassignable and redeclarable;
let=block scoped, reassignble but not redeclarable;
const=block scoped, not reassignable and not redeclarable.

2.What is the spread operator?
and=>
spread operator is javaScript code it's use for (array and object) copy data, individual data and so on.

3.What is the difference between map(),filter() and forEach()?
ans=>Thes are javaScript code map() use for get data from array and give us new array,
filter() usr for get unique data and give new array
forEach() use for get data and not give us new array

4.ans=>A number of difference of traditional function and arrow function. Arrow function is shorter more then traditional function and it's update version

5.What are template literals?
ans=> literals is an important and very useful code of javaScript such as backtick(``),dolursign(${}),multilie(a/b) and so on.
